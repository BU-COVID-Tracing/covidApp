import { ResizeMode, Parameters } from '../constants';
export default function configureIos(projectRootPath: string, { resizeMode, backgroundColor, darkModeBackgroundColor, imagePath, darkModeImagePath, }: Parameters & {
    resizeMode: ResizeMode;
}): Promise<void>;
